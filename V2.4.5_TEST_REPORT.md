# V2.4.5 本機驗證報告

驗證日期：2026-06-16  
驗證範圍：上傳基準 ZIP 的本機工作副本  
外部連線：未執行  
正式部署：未執行  
Firebase／Firestore 寫入：未執行

## 已通過

1. `index.html` 內 ES Module 經 `node --check` 通過。
2. `scripts/update-public-stats.mjs` 經 `node --check` 通過。
3. `package.json`、`package-lock.json`、`manifest.webmanifest` 可正常解析為 JSON。
4. 全站目前版本、Open Graph、Twitter Meta 與統計腳本固定版本已統一為 V2.4.5。
5. 已移除 Level 20 以上固定回到 Level 21 的啟動條件。
6. 無正式棋盤存檔時，續玩改從完整成績快照同時恢復 `score`、`level`、`lines`。
7. 本機排行榜與世界盃候選不再分別取三個欄位最大值後拼接。
8. Game Over 會先顯示可操作罩幕，再執行排行榜與同步流程；`showOverlay()` 會恢復 `visibility: visible`。
9. 暫停／Game Over 返回首頁、全新挑戰、扣分重挑、扣技能重挑及鍵盤 R 均有二次確認入口。
10. 扣分不足需退回上一關時，不會再出現第三次原生確認視窗。
11. `firestore.rules` 與 `.github/workflows/update-public-stats.yml` 和基準檔位元完全一致，未被修改。

## 尚未執行

- 真實 iPhone／Android 的觸控與版面測試。
- 高等級真實帳號 Game Over 實機測試。
- Firebase Authentication、Firestore 寫入與世界盃同步測試。
- GitHub Pages 部署測試。
- LINE 伺服器重新抓取分享預覽的測試。

## 已知限制

- 現存的歷史錯置資料不會自動猜測或改寫。
- LINE 可能保留舊預覽快取；部署 V2.4.5 後仍須以新訊息或帶查詢參數的網址驗證。
- 本報告是靜態與語法驗證，不等同正式環境驗收。
